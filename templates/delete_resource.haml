%p<
  Really delete
  %a{:href => url_read}= name
  ?

%p
  The content will not be permanently lost, but there is currently no
  way to restore it via the web interface.

%form{:method => "post", :action => url_submit}
  %input(type="hidden" name="_method" value="delete")
  %input(type="submit" value="Delete")
  %a{:href => url_read} Cancel
